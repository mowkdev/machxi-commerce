DATABASE_URL=postgresql://postgres:postgres@localhost:5432/machxi_commerce

# pnpm db:seed — initial admin user (change password for non-local use)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme

# apps/admin — base URL for the API (must be an origin allowed by CORS_ORIGIN)
VITE_API_URL=http://localhost:3001
