/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "../../runtime";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../runtime";
import type {
  AdminExpireCartMutationResponse,
  AdminExpireCartPathParams,
  AdminExpireCart400,
  AdminExpireCart401,
  AdminExpireCart403,
  AdminExpireCart404,
  AdminExpireCart409,
  AdminExpireCart500,
} from "../types/AdminExpireCart.ts";
import { adminExpireCartMutationResponseSchema } from "../zod/adminExpireCartSchema.ts";

function getAdminExpireCartUrl(id: AdminExpireCartPathParams["id"]) {
  const res = { method: "POST", url: `/api/carts/${id}/expire` as const };
  return res;
}

/**
 * @summary Expire a cart and release its inventory reservations
 * {@link /api/carts/:id/expire}
 */
export async function adminExpireCart(
  id: AdminExpireCartPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AdminExpireCartMutationResponse,
    ResponseErrorConfig<
      | AdminExpireCart400
      | AdminExpireCart401
      | AdminExpireCart403
      | AdminExpireCart404
      | AdminExpireCart409
      | AdminExpireCart500
    >,
    unknown
  >({
    method: "POST",
    url: getAdminExpireCartUrl(id).url.toString(),
    ...requestConfig,
  });
  return adminExpireCartMutationResponseSchema.parse(res.data);
}
