NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_DEFAULT_CURRENCY=EUR
# Optional fallback if Stripe publishable key is not stored on the payment provider config
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
